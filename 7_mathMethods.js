/*
finding a random element in an array=============================================
const arr = ['this', 'is', 'super', 'story'];
// to get a random value from d array
const randIndex = Math.floor(arr.length * Math.random());
console.log(arr[randIndex])
*/
